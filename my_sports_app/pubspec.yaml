name: my_sports_app # ชื่อโปรเจกต์ของคุณ
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  provider: ^6.0.5 
  uuid: ^4.1.0 
  google_fonts: ^6.2.1
  # สำหรับ Localization (การรองรับหลายภาษา)
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2 

  # สำหรับบันทึกค่าภาษาที่ผู้ใช้เลือก (Shared Preferences)
  shared_preferences: ^2.2.0 # <<<<<<<<<<<< เพิ่มบรรทัดนี้

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true # <<<<<<<<<<<< บรรทัดนี้ถูกต้องแล้ว